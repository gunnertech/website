build:
  artifacts:
    baseDirectory: /gatsby
    discardPaths: true
    files:
      - '**/*'
  cache:
    paths: []
  phases:
    pre_build:
      commands: [
          'ls .',
          'yarn install'
      ]
    build:
      commands: ['yarn build']
    post_build:
      commands: []
env:
  variables: []
version: 0.1
